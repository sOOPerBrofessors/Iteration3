package View.AreaViewport;

/**
 * Created by dyeung on 4/10/16.
 */

//Purpose of this class is for creating a generic offset to center the entities on the tile and place tiles next to each other
public class OffsetFactory {/*
    public static int tileWidth = Settings.TILEWIDTH;
    public static int tileHeight = Settings.TILEHEIGHT;

    //The two functions will return the x and y pixel position for where it will be placed on the map
    public static Pair<Integer, Integer> centerEntityOnTile(Location location, Entity entity){
        int x = 0;
        int y = 0;
        return new Pair<>(x,y);
    }
    public static Pair<Integer, Integer> offsetTiles(Location location){
        int x = location.getX();
        int y = location.getY();
        int xPixel = x*tileWidth - (x*(tileWidth))/4;
        int yPixel = y*(tileHeight - 5) + ((tileHeight-7)*x)/2;

        return new Pair<>(xPixel,yPixel);
    }*/
}
